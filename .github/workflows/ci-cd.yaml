name: CI/CD Pipeline
on:
  push:
    branches: ["main"]
  pull_request:

jobs:
  lint-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Lint Manifests
        run: kubectl kustomize k8s/overlays/dev --validate
